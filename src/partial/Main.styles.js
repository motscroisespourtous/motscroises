import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    backgroundColor: '#85d4ff3d',
    paddingTop: '60px',
    paddingLeft: '24px',
  },
});

export { useStyles };